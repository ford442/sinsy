{
  "name": "sinsy",
  "version": "0.92.0",
  "private": true,
  "scripts": {
    "build": "emconfigure ./configure && emmake make",
    "dev": "npm run build && http-server -c-1 ."
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
