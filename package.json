{
  "name": "sinsy",
  "version": "0.92.0",
  "private": true,
  "scripts": {
    "build": "source /content/build_space/emsdk/emsdk_env.sh && emconfigure ./configure && emmake make",
    "dev": "npm run build && http-server -c-1 ."
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
